ServiceReference<IRuleProvider> serviceReference = KampRuleLanguageFacade.getServiceReference();
if(serviceReference != null) {
	BundleContext bundleContext = FrameworkUtil.getBundle(getClass()).getBundleContext();
	IRuleProvider service = bundleContext.getService(serviceReference);
	
	// call the rules
	System.err.println(service.getRule());
	
	KampRuleLanguageFacade.unregisterService(serviceReference);
}